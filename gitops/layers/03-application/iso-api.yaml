apiVersion: apps/v1
kind: Deployment
metadata:
  name: iso-api-orchestrator
  labels:
    app.kubernetes.io/name: iso-api-orchestrator
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: iso-api-orchestrator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iso-api-orchestrator
    spec:
      serviceAccountName: iso-platform
      containers:
        - name: iso-api-orchestrator
          image: image-registry.openshift-image-registry.svc:5000/iso-platform/iso-api-orchestrator:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: iso-app-config
          env:
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: iso-secrets
                  key: DATABASE_PASSWORD
            - name: MAAS_BEARER_TOKEN_FILE
              value: /var/run/secrets/maas/token
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: iso-secrets
                  key: GOOGLE_CLIENT_SECRET
                  optional: true
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: iso-secrets
                  key: JWT_SECRET
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: iso-secrets
                  key: GOOGLE_CLIENT_ID
                  optional: true
            - name: SAML_IDP_ENTITY_ID
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: SAML_IDP_ENTITY_ID
                  optional: true
            - name: SAML_IDP_SSO_URL
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: SAML_IDP_SSO_URL
                  optional: true
            - name: SAML_SP_ENTITY_ID
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: SAML_SP_ENTITY_ID
                  optional: true
            - name: SAML_SP_ACS_URL
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: SAML_SP_ACS_URL
                  optional: true
            - name: SAML_IDP_X509_CERT
              valueFrom:
                secretKeyRef:
                  name: iso-secrets
                  key: SAML_IDP_X509_CERT
                  optional: true
            - name: AUTH_DEV_MODE
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: AUTH_DEV_MODE
            - name: ADMIN_EMAILS
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: ADMIN_EMAILS
            - name: ISO_WEB_URL
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: ISO_WEB_URL
            - name: DOCGEN_URL
              valueFrom:
                configMapKeyRef:
                  name: iso-app-config
                  key: DOCGEN_URL
          volumeMounts:
            - name: maas-token
              mountPath: /var/run/secrets/maas
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
      volumes:
        - name: maas-token
          projected:
            sources:
              - serviceAccountToken:
                  audience: maas-default-gateway-sa
                  expirationSeconds: 3600
                  path: token
---
apiVersion: v1
kind: Service
metadata:
  name: iso-api-orchestrator
  labels:
    app.kubernetes.io/name: iso-api-orchestrator
spec:
  selector:
    app.kubernetes.io/name: iso-api-orchestrator
  ports:
    - port: 8080
      targetPort: http
      name: http
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: iso-api-orchestrator
  labels:
    app.kubernetes.io/name: iso-api-orchestrator
  annotations:
    haproxy.router.openshift.io/timeout: 300s
    haproxy.router.openshift.io/max-body-size: 100m
spec:
  to:
    kind: Service
    name: iso-api-orchestrator
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
