# Example only — copy to iso-secrets.yaml locally (gitignored) and apply with real values.
apiVersion: v1
kind: Secret
metadata:
  name: iso-secrets
  labels:
    app.kubernetes.io/component: app-infra
type: Opaque
stringData:
  DATABASE_PASSWORD: "CHANGE_ME_iso_dev_password"
  LLM_API_KEY: "REPLACE_MaaS_TOKEN"
  GIT_USERNAME: "x-access-token"
  GIT_PASSWORD: "REPLACE_GITHUB_PAT_FOR_PRIVATE_CLONE"
  GOOGLE_CLIENT_SECRET: "REPLACE_GOOGLE_OAUTH_SECRET"
  JWT_SECRET: "REPLACE_JWT_SIGNING_SECRET"
